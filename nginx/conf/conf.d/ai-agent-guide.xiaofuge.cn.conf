# HTTP → HTTPS 重定向
server {
    listen 80;
    listen [::]:80;
    server_name ai-agent-guide.xiaofuge.cn;
    rewrite ^(.*) https://$server_name$1 permanent;
}

# HTTPS 主服务
server {
    listen       443 ssl;
    server_name  ai-agent-guide.xiaofuge.cn;

    ssl_certificate      /etc/nginx/ssl/_.xiaofuge.cn.pem;
    ssl_certificate_key  /etc/nginx/ssl/_.xiaofuge.cn.key;

    ssl_session_cache    shared:SSL:1m;
    ssl_session_timeout  5m;

    ssl_ciphers  HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers  on;

    root /usr/share/nginx/html/ai-agent-guide.xiaofuge.cn;
    index index.html;

    # 字符编码
    charset utf-8;

    # 静态资源缓存
    location ~* \.(css|js|json|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 7d;
        add_header Cache-Control "public, no-transform";
    }

    # chapters 目录
    location /chapters/ {
        try_files $uri $uri/ /index.html;
    }

    # SPA 路由回退
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 错误页面
    error_page 404 /index.html;
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
