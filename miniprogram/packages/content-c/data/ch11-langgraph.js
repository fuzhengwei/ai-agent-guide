module.exports = {"key":"c19","slug":"ch11-langgraph","title":"第19章 LangGraph 与状态机","subtitle":"用图结构构建可控的 Agent 工作流","nodes":[{"type":"h","level":2,"text":"19.1 LangGraph：把 Agent 流程变成图","anchor":"h-0","tts":0},{"type":"p","text":"LangChain 团队推出的 LangGraph 是 2024 年最火的 Agent 框架之一。核心思想：把 Agent 工作流建模为有向图。","sub":false,"tts":1},{"type":"quote","text":"Agent 的每一步是图中的一个节点（Node），步骤之间的跳转是边（Edge）。整个 Agent 就是一张图，可视化、可调试、可控。","tts":2},{"type":"flow","steps":[{"kind":"start","label":"START / (入口节点)","edge":"","branch":false},{"kind":"process","label":"规划节点 / (planner)","edge":"","branch":false},{"kind":"tool","label":"搜索节点 / (searcher)","edge":"需搜索","branch":false},{"kind":"decision","label":"检查节点 / (reviewer)","edge":"","branch":false},{"kind":"end","label":"END / (出口节点)","edge":"完成","branch":false},{"kind":"tool","label":"代码节点 / (coder)","edge":"","branch":true}]},{"type":"h","level":2,"text":"19.2 LangGraph 的核心概念","anchor":"h-1","tts":3},{"type":"card","title":"📐 五个核心概念","accent":"","children":[{"type":"p","text":"1. State（状态）","tts":4},{"type":"p","text":"全局状态对象，所有节点共享。用 TypedDict 定义，包含消息历史、中间结果等。每个节点可以读写 State。","sub":false,"tts":5},{"type":"p","text":"2. Node（节点）","tts":6},{"type":"p","text":"图中的执行单元。每个节点是一个函数，接收 State，返回更新后的 State。节点 = 一步操作。","sub":false,"tts":7},{"type":"p","text":"3. Edge（边）","tts":8},{"type":"p","text":"节点之间的连接。普通边 = 固定跳转；条件边 = 根据 State 动态决定下一个节点。","sub":false,"tts":9},{"type":"p","text":"4. Conditional Edge（条件边）","tts":10},{"type":"p","text":"根据当前 State 动态路由。类似 if-else：if state['need_search'] → search_node, else → code_node。","sub":false,"tts":11},{"type":"p","text":"5. Checkpoint（检查点）","tts":12},{"type":"p","text":"每个节点执行后自动保存 State 快照。支持时间旅行（回到任意检查点）、人工干预（暂停在某节点等人审核后继续）、错误恢复（从失败点重试）。","sub":false,"tts":13}],"qa":false},{"type":"h","level":2,"text":"19.3 用 LangGraph 构建 ReAct Agent","anchor":"h-2","tts":14},{"type":"code","lang":"Python","code":"from langgraph.graph import StateGraph, END\nfrom typing import TypedDict, Annotated\nfrom langchain_core.messages import HumanMessage, AIMessage\nimport operator\n\n# 1. 定义状态\nclass AgentState(TypedDict):\n    messages: Annotated[list, operator.add]  # 消息列表（只增不覆盖）\n    tool_results: list  # 工具结果\n\n# 2. 定义节点函数\ndef call_llm(state: AgentState):\n    \"\"\"LLM 推理节点：决定下一步做什么\"\"\"\n    response = llm.invoke(state[\"messages\"])\n    return {\"messages\": [response]}\n\ndef call_tool(state: AgentState):\n    \"\"\"工具执行节点：执行 LLM 选择的工具\"\"\"\n    last_msg = state[\"messages\"][-1]\n    tool_call = last_msg.tool_calls[0]\n    result = execute_tool(tool_call)\n    return {\"messages\": [ToolMessage(result)]}\n\n# 3. 定义条件路由\ndef should_continue(state: AgentState):\n    \"\"\"条件边：判断是否继续循环\"\"\"\n    last_msg = state[\"messages\"][-1]\n    if last_msg.tool_calls:\n        return \"tools\"  # 需要调用工具\n    return END           # 不需要，结束\n\n# 4. 构建图\nworkflow = StateGraph(AgentState)\n\n# 添加节点\nworkflow.add_node(\"agent\", call_llm)\nworkflow.add_node(\"tools\", call_tool)\n\n# 设置入口\nworkflow.set_entry_point(\"agent\")\n\n# 添加边和条件边\nworkflow.add_conditional_edges(\n    \"agent\",        # 源节点\n    should_continue, # 路由函数\n    {\n        \"tools\": \"tools\",  # 返回 \"tools\" → 跳到 tools 节点\n        END: END           # 返回 END → 结束\n    }\n)\nworkflow.add_edge(\"tools\", \"agent\")  # tools 执行完回到 agent\n\n# 5. 编译并运行\napp = workflow.compile(checkpointer=MemorySaver())\n\nresult = app.invoke(\n    {\"messages\": [HumanMessage(\"北京明天天气？\")]},\n    config={\"configurable\": {\"thread_id\": \"1\"}}\n)"},{"type":"h","level":3,"text":"代码解析","anchor":"h-3","tts":15},{"type":"card","title":"","accent":"","children":[{"type":"p","text":"State 定义","tts":16},{"type":"p","text":"Annotated[list, operator.add] 表示 messages 列表用\"追加\"而非\"覆盖\"语义。新消息追加到列表，不替换旧消息。","sub":false,"tts":17},{"type":"p","text":"条件边","tts":18},{"type":"p","text":"should_continue 函数检查最后一条消息是否包含 tool_calls。有则路由到 tools 节点，无则结束。","sub":false,"tts":19},{"type":"p","text":"循环结构","tts":20},{"type":"p","text":"agent → (条件) → tools → agent → ... 形成循环。这就是 ReAct 的循环在 LangGraph 中的表达。","sub":false,"tts":21},{"type":"p","text":"Checkpointer","tts":22},{"type":"p","text":"MemorySaver() 保存每步状态。通过 thread_id 可以回到任意检查点，支持人工干预。","sub":false,"tts":23}],"qa":false},{"type":"h","level":2,"text":"19.4 Human-in-the-Loop：人工干预","anchor":"h-4","tts":24},{"type":"p","text":"LangGraph 的 Checkpoint 机制让\"人工干预\"变得自然：","sub":false,"tts":25},{"type":"flow","steps":[{"kind":"process","label":"Agent 执行","edge":"","branch":false},{"kind":"decision","label":"检查点 / (暂停)","edge":"","branch":false},{"kind":"process","label":"人工审核 / (Human)","edge":"","branch":false},{"kind":"decision","label":"通过?","edge":"","branch":false},{"kind":"process","label":"继续执行","edge":"通过","branch":false},{"kind":"process","label":"修改 State / 后继续","edge":"","branch":true},{"kind":"end","label":"终止","edge":"","branch":true}]},{"type":"code","lang":"人工干预代码示例","code":"# 在工具执行前暂停，等待人工审核\napp = workflow.compile(\n    checkpointer=MemorySaver(),\n    interrupt_before=[\"tools\"]  # 在 tools 节点前暂停\n)\n\n# 第一次调用 → 执行到 tools 前暂停\nresult = app.invoke(\n    {\"messages\": [HumanMessage(\"删除 /tmp 目录\")]},\n    config={\"configurable\": {\"thread_id\": \"1\"}}\n)\n# 此时 Agent 想调用 rm 命令，暂停等待人工审核\n\n# 人工审核后，决定继续或修改\n# 方式1: 直接继续（批准）\nresult = app.invoke(None, config={\"configurable\": {\"thread_id\": \"1\"}})\n\n# 方式2: 修改 State 后继续（纠正）\napp.update_state(\n    config={\"configurable\": {\"thread_id\": \"1\"}},\n    values={\"messages\": [HumanMessage(\"不要删除，改为列出文件\")]}\n)\nresult = app.invoke(None, config={\"configurable\": {\"thread_id\": \"1\"}})"},{"type":"h","level":2,"text":"19.5 LangGraph vs LangChain Agent","anchor":"h-5","tts":26},{"type":"card","title":"","accent":"","children":[{"type":"table","rows":[{"head":true,"cells":["对比项","LangChain Agent","LangGraph"]},{"head":false,"cells":["流程控制","黑盒，LLM 自主决定","白盒，图结构显式定义"]},{"head":false,"cells":["可调试性","难，只能看最终输出","强，每步可检查 State"]},{"head":false,"cells":["人工干预","不支持","原生支持（Checkpoint）"]},{"head":false,"cells":["并行执行","不支持","支持（多个节点并行）"]},{"head":false,"cells":["适用场景","简单、快速的 Agent","复杂、需要控制的生产级 Agent"]}]}],"qa":false},{"type":"h","level":2,"text":"19.6 LangGraph 完整实战代码","anchor":"h-6","tts":27},{"type":"p","text":"前面几节我们分别学习了 State、Node、Edge 和 Checkpoint 的概念，现在把它们组合起来，写一个完整可运行的 LangGraph 项目。这个示例模拟一个智能客服 Agent：接收用户问题，判断是否需要查数据库、调用外部 API 或直接回答，执行后在审查节点检查结果质量，不达标则重试，达标则返回最终回复。","sub":false,"tts":28},{"type":"code","lang":"Python","code":"from langgraph.graph import StateGraph, END, START\nfrom langgraph.checkpoint.memory import MemorySaver\nfrom langgraph.prebuilt import ToolNode\nfrom typing import TypedDict, Annotated\nimport operator\n\n# ── 1. 定义全局状态 ──\nclass AgentState(TypedDict):\n    messages: Annotated[list, operator.add]   # 对话历史（追加策略）\n    step_count: int                            # 当前循环步数\n    tool_results: list                         # 工具返回结果\n    needs_review: bool                         # 是否需要人工审查\n\n# ── 2. 定义节点函数 ──\ndef agent_node(state: AgentState) -> dict:\n    \"\"\"LLM 推理节点：分析用户意图，决定调用哪个工具\"\"\"\n    step = state.get(\"step_count\", 0) + 1\n    # 实际项目中这里调用 llm.invoke(state[\"messages\"])\n    decision = \"search_db\" if step < 3 else \"answer\"\n    return {\"messages\": [{\"role\": \"ai\", \"content\": f\"step={step}, decision={decision}\"}], \"step_count\": step}\n\ndef search_db_node(state: AgentState) -> dict:\n    \"\"\"数据库查询节点：根据 LLM 决策检索知识库\"\"\"\n    # 实际项目中这里连接数据库执行查询\n    result = {\"found\": True, \"data\": \"订单 #12345 已发货，预计 7月3日 送达\"}\n    return {\"tool_results\": [result], \"messages\": [{\"role\": \"tool\", \"content\": str(result)}]}\n\ndef answer_node(state: AgentState) -> dict:\n    \"\"\"最终回复节点：整合所有信息生成回答\"\"\"\n    final = f\"根据查询结果：{state.get('tool_results', [])}\"\n    return {\"messages\": [{\"role\": \"ai\", \"content\": final}]}\n\ndef review_node(state: AgentState) -> dict:\n    \"\"\"质量审查节点：检查回复是否满足要求\"\"\"\n    last = state[\"messages\"][-1]\n    quality_ok = len(str(last.get(\"content\", \"\"))) > 5\n    return {\"needs_review\": not quality_ok, \"step_count\": state.get(\"step_count\", 0)}\n\n# ── 3. 定义条件路由 ──\ndef should_continue(state: AgentState) -> str:\n    if state.get(\"step_count\", 0) >= 5:\n        return \"end\"\n    last_msg = state[\"messages\"][-1]\n    if \"decision=search_db\" in str(last_msg.get(\"content\", \"\")):\n        return \"search_db\"\n    return \"answer\"\n\ndef review_decision(state: AgentState) -> str:\n    if state.get(\"needs_review\", False) and state.get(\"step_count\", 0) < 5:\n        return \"retry\"\n    return \"finish\"\n\n# ── 4. 构建 StateGraph ──\nworkflow = StateGraph(AgentState)\nworkflow.add_node(\"agent\", agent_node)\nworkflow.add_node(\"search_db\", search_db_node)\nworkflow.add_node(\"answer\", answer_node)\nworkflow.add_node(\"review\", review_node)\n\nworkflow.add_edge(START, \"agent\")\nworkflow.add_conditional_edges(\"agent\", should_continue, {\n    \"search_db\": \"search_db\",\n    \"answer\": \"answer\",\n    \"end\": END\n})\nworkflow.add_edge(\"search_db\", \"review\")\nworkflow.add_edge(\"answer\", \"review\")\nworkflow.add_conditional_edges(\"review\", review_decision, {\n    \"retry\": \"agent\",\n    \"finish\": END\n})\n\n# ── 5. 编译并运行 ──\nmemory = MemorySaver()\napp = workflow.compile(checkpointer=memory)\n\nresult = app.invoke(\n    {\"messages\": [{\"role\": \"user\", \"content\": \"我的订单 #12345 到哪了？\"}], \"step_count\": 0, \"tool_results\": [], \"needs_review\": False},\n    config={\"configurable\": {\"thread_id\": \"customer-001\"}}\n)\nprint(result[\"messages\"][-1])"},{"type":"p","text":"上面这段代码展示了 LangGraph 的完整开发流程：定义 State → 编写节点函数 → 定义条件路由 → 组装图 → 编译运行。几个关键设计点值得注意：第一，step_count 字段用于防止无限循环，当步数超过 5 时强制结束，这是生产环境中的必备安全措施。第二，review 节点充当质量守门员，不满意的回复会被打回 agent 节点重试，形成了一个自我纠错的闭环。第三，MemorySaver 配合 thread_id 实现了状态持久化，同一个 thread 的多次调用可以无缝续接，这对于多轮对话场景至关重要。","sub":false,"tts":29},{"type":"p","text":"在实际项目中，你只需要把 agent_node 中的伪逻辑替换为真实的 LLM 调用，把 search_db_node 替换为你的数据库查询逻辑，就可以直接部署使用了。整个图结构的定义不需要改动，这就是 LangGraph 「一次建模、反复使用」的优势所在。","sub":false,"tts":30},{"type":"h","level":2,"text":"19.7 LangGraph vs 其他框架对比","anchor":"h-7","tts":31},{"type":"p","text":"除了 LangGraph，市面上还有多个流行的 Agent 框架，它们各有侧重。理解差异有助于在项目中做出正确的技术选型。下面从五个关键维度对比四大主流框架。","sub":false,"tts":32},{"type":"card","title":"","accent":"","children":[{"type":"table","rows":[{"head":true,"cells":["对比维度","LangGraph","LangChain Agent","AutoGen","CrewAI"]},{"head":false,"cells":["图结构支持","原生有向图，支持环和并行","无图结构，链式调用","对话图，以消息传递为核心","任务图，按角色分工串联"]},{"head":false,"cells":["状态管理","TypedDict 全局 State + Checkpoint","仅消息历史，无持久状态","对话上下文即状态","任务结果传递，无统一 State"]},{"head":false,"cells":["人在循环（HITL）","原生支持，interrupt_before/after","不支持","支持，通过 human_reply 消息","有限支持，需自定义逻辑"]},{"head":false,"cells":["并行执行","支持，多节点可并行触发","不支持，严格串行","支持，多 Agent 可并行对话","有限支持，任务级别可并行"]},{"head":false,"cells":["适用场景","复杂生产级 Agent、需要精细控制","快速原型、简单链式任务","多 Agent 对话协作、研究探索","角色扮演型团队协作、SOP 驱动"]}]}],"qa":false},{"type":"p","text":"从对比可以看出，LangGraph 在图结构表达力和状态可控性方面优势明显：它既支持循环（ReAct 模式）又支持并行（多节点同时触发），加上 Checkpoint 机制让 Human-in-the-Loop 变得开箱即用。AutoGen 更适合多 Agent 自由对话的研究场景，CrewAI 则擅长按预设角色分工的任务流水线。选型建议：需要精细控制流程选 LangGraph，快速验证选 LangChain，多 Agent 协作选 AutoGen，角色分工明确选 CrewAI。","sub":false,"tts":33},{"type":"flow","steps":[{"kind":"start","label":"LangGraph / 复杂度 ★★★★★ / 控制力 最强","edge":"","branch":false},{"kind":"process","label":"AutoGen / 复杂度 ★★★★ / 控制力 较强","edge":"复杂→简单","branch":false},{"kind":"process","label":"CrewAI / 复杂度 ★★★ / 控制力 中等","edge":"","branch":false},{"kind":"process","label":"LangChain / 复杂度 ★★ / 控制力 较弱","edge":"","branch":false},{"kind":"end","label":"END / 根据场景选择 / 合适框架","edge":"","branch":false}]},{"type":"h","level":2,"text":"19.8 高级特性：子图与人机协同","anchor":"h-8","tts":34},{"type":"h","level":3,"text":"子图（Subgraph）：模块化管理复杂流程","anchor":"h-9","tts":35},{"type":"p","text":"当 Agent 工作流变得复杂时，把所有节点放在一个图里会导致可读性和可维护性下降。子图允许你把一组相关节点封装成一个独立子图，然后作为单个节点嵌入主图。这样做有三个好处：一是逻辑分层，主图只关心高层流程，子图处理细节；二是复用性，同一个子图可以在不同主图中重复使用；三是独立测试，子图可以单独编译和调试，不影响主图。","sub":false,"tts":36},{"type":"code","lang":"Python","code":"from langgraph.graph import StateGraph, END, START\nfrom typing import TypedDict, Annotated\nimport operator\n\n# ── 子图状态：研究子流程 ──\nclass ResearchState(TypedDict):\n    queries: list          # 查询列表\n    findings: Annotated[list, operator.add]  # 研究结果\n\n# ── 子图节点 ──\ndef generate_queries(state: ResearchState) -> dict:\n    \"\"\"根据用户问题生成多个搜索查询\"\"\"\n    return {\"queries\": [\"LangGraph 教程\", \"LangGraph 实战\", \"LangGraph vs LangChain\"]}\n\ndef execute_search(state: ResearchState) -> dict:\n    \"\"\"执行搜索并收集结果\"\"\"\n    results = []\n    for q in state[\"queries\"]:\n        # 实际项目中调用搜索 API\n        results.append({\"query\": q, \"snippet\": f\"关于 {q} 的搜索结果...\"})\n    return {\"findings\": results}\n\ndef summarize(state: ResearchState) -> dict:\n    \"\"\"汇总搜索结果\"\"\"\n    summary = \"; \".join([r[\"snippet\"] for r in state[\"findings\"]])\n    return {\"findings\": [{\"summary\": summary}]}\n\n# ── 构建子图 ──\nresearch_subgraph = StateGraph(ResearchState)\nresearch_subgraph.add_node(\"gen_queries\", generate_queries)\nresearch_subgraph.add_node(\"search\", execute_search)\nresearch_subgraph.add_node(\"summarize\", summarize)\nresearch_subgraph.add_edge(START, \"gen_queries\")\nresearch_subgraph.add_edge(\"gen_queries\", \"search\")\nresearch_subgraph.add_edge(\"search\", \"summarize\")\nresearch_subgraph.add_edge(\"summarize\", END)\nresearch_app = research_subgraph.compile()\n\n# ── 主图：将子图作为节点嵌入 ──\nclass MainState(TypedDict):\n    topic: str\n    research_result: str\n    final_report: str\n\ndef research_node(state: MainState) -> dict:\n    \"\"\"调用研究子图\"\"\"\n    sub_result = research_app.invoke({\"queries\": [], \"findings\": []})\n    return {\"research_result\": str(sub_result[\"findings\"][-1])}\n\ndef write_report(state: MainState) -> dict:\n    \"\"\"根据研究结果撰写报告\"\"\"\n    return {\"final_report\": f\"研究报告：{state['research_result']}\"}\n\nmain_graph = StateGraph(MainState)\nmain_graph.add_node(\"research\", research_node)\nmain_graph.add_node(\"report\", write_report)\nmain_graph.add_edge(START, \"research\")\nmain_graph.add_edge(\"research\", \"report\")\nmain_graph.add_edge(\"report\", END)\nmain_app = main_graph.compile()"},{"type":"p","text":"这段代码展示了两层图结构：内层 research_subgraph 负责「生成查询 → 执行搜索 → 汇总结果」三步，外层主图调用子图后将结果传给 write_report 节点生成最终报告。子图的 State（ResearchState）与主图的 State（MainState）完全独立，通过 research_node 函数做桥接转换，这种设计让两层的逻辑互不污染。","sub":false,"tts":37},{"type":"h","level":3,"text":"Human-in-the-Loop：三种人机协同模式","anchor":"h-10","tts":38},{"type":"p","text":"LangGraph 的人机协同不止「暂停-审核-继续」一种模式，实际项目中有三种常见模式：审批模式（执行前等人批准）、纠正模式（暂停后人工修改 State 再继续）、对话模式（Agent 遇到不确定时主动向人提问）。下面用代码演示这三种模式的实现。","sub":false,"tts":39},{"type":"code","lang":"Python","code":"from langgraph.graph import StateGraph, END, START\nfrom langgraph.checkpoint.memory import MemorySaver\nfrom typing import TypedDict\n\nclass HitlState(TypedDict):\n    action: str\n    approved: bool\n    feedback: str\n    result: str\n\n# ── 模式1：审批模式 — 执行前等待人工批准 ──\ndef risky_action(state: HitlState) -> dict:\n    \"\"\"高风险操作节点（如删除文件、发送邮件）\"\"\"\n    return {\"result\": f\"已执行操作: {state['action']}\"}\n\ndef execute_approval_mode():\n    \"\"\"编译时设置 interrupt_before，在 risky_action 前暂停\"\"\"\n    wg = StateGraph(HitlState)\n    wg.add_node(\"risky\", risky_action)\n    wg.add_edge(START, \"risky\")\n    wg.add_edge(\"risky\", END)\n    app = wg.compile(\n        checkpointer=MemorySaver(),\n        interrupt_before=[\"risky\"]  # 关键：执行前暂停\n    )\n    return app\n\n# ── 模式2：纠正模式 — 人工修改 State 后继续 ──\ndef execute_correction_mode(app, thread_id: str):\n    \"\"\"暂停后人工修改 State，再继续执行\"\"\"\n    cfg = {\"configurable\": {\"thread_id\": thread_id}}\n    # 第一次调用：执行到暂停点\n    state = app.invoke({\"action\": \"删除 /tmp/all\", \"approved\": False, \"feedback\": \"\", \"result\": \"\"}, config=cfg)\n    # 人工审查后修改 State\n    app.update_state(cfg, values={\"action\": \"列出 /tmp/all 文件而非删除\"})\n    # 继续执行\n    result = app.invoke(None, config=cfg)\n    return result\n\n# ── 模式3：对话模式 — Agent 主动向人提问 ──\ndef ask_human(state: HitlState) -> dict:\n    \"\"\"Agent 遇到不确定时，生成问题等待人类回答\"\"\"\n    question = \"我不确定你想删除哪个目录，请指定：\"\n    # 实际项目中通过 UI 或 chat 将问题发给用户\n    return {\"result\": question, \"feedback\": \"等待用户回答\"}\n\ndef execute_dialog_mode():\n    \"\"\"Agent 在条件边中判断不确定时跳转到 ask_human 节点\"\"\"\n    wg = StateGraph(HitlState)\n    wg.add_node(\"risky\", risky_action)\n    wg.add_node(\"ask\", ask_human)\n    wg.add_edge(START, \"risky\")\n    # 条件边：不确定时跳到 ask 节点\n    wg.add_conditional_edges(\"risky\", lambda s: \"ask\" if not s.get(\"approved\") else END,\n                            {\"ask\": \"ask\", END: END})\n    wg.add_edge(\"ask\", END)\n    return wg.compile(checkpointer=MemorySaver())"},{"type":"p","text":"三种模式各有适用场景：审批模式适合高风险操作（如删除数据、发送邮件），纠正模式适合 Agent 理解有偏差时人工修正，对话模式适合 Agent 信息不足需要向人补充询问。生产项目中可以组合使用，比如先对话模式收集信息，再审批模式确认执行。","sub":false,"tts":40},{"type":"h","level":3,"text":"Streaming：流式输出实时反馈","anchor":"h-11","tts":41},{"type":"p","text":"用户体验角度，长时间运行的 Agent 必须提供实时反馈，否则用户会以为程序卡住了。LangGraph 原生支持 Streaming，可以流式输出每个节点的结果、State 变化以及 LLM 的 token 级流式输出。","sub":false,"tts":42},{"type":"code","lang":"Python","code":"from langgraph.graph import StateGraph, END, START\nfrom langgraph.checkpoint.memory import MemorySaver\nfrom typing import TypedDict, Annotated\nimport operator\n\nclass StreamState(TypedDict):\n    messages: Annotated[list, operator.add]\n    current_step: str\n\ndef thinking_node(state: StreamState) -> dict:\n    \"\"\"模拟 LLM 思考过程\"\"\"\n    return {\"messages\": [{\"role\": \"ai\", \"content\": \"正在分析问题...\"}], \"current_step\": \"thinking\"}\n\ndef searching_node(state: StreamState) -> dict:\n    \"\"\"模拟搜索过程\"\"\"\n    return {\"messages\": [{\"role\": \"tool\", \"content\": \"找到 3 条相关结果\"}], \"current_step\": \"searching\"}\n\ndef responding_node(state: StreamState) -> dict:\n    \"\"\"生成最终回复\"\"\"\n    return {\"messages\": [{\"role\": \"ai\", \"content\": \"基于搜索结果，答案是...\"}], \"current_step\": \"responding\"}\n\n# 构建图\nwg = StateGraph(StreamState)\nwg.add_node(\"think\", thinking_node)\nwg.add_node(\"search\", searching_node)\nwg.add_node(\"respond\", responding_node)\nwg.add_edge(START, \"think\")\nwg.add_edge(\"think\", \"search\")\nwg.add_edge(\"search\", \"respond\")\nwg.add_edge(\"respond\", END)\n\napp = wg.compile(checkpointer=MemorySaver())\n\n# ── 方式1：stream 模式 — 逐节点输出 ──\nprint(\"=== stream 模式 ===\")\nfor event in app.stream(\n    {\"messages\": [{\"role\": \"user\", \"content\": \"什么是 LangGraph？\"}], \"current_step\": \"\"},\n    config={\"configurable\": {\"thread_id\": \"stream-001\"}}\n):\n    node_name = list(event.keys())[0]\n    print(f\"[{node_name}] → {event[node_name]}\")\n\n# ── 方式2：stream_values 模式 — 每步输出完整 State ──\nprint(\"\\n=== stream_values 模式 ===\")\nfor state in app.stream_values(\n    {\"messages\": [{\"role\": \"user\", \"content\": \"什么是 LangGraph？\"}], \"current_step\": \"\"},\n    config={\"configurable\": {\"thread_id\": \"stream-002\"}}\n):\n    print(f\"current_step={state.get('current_step')}, messages_count={len(state.get('messages', []))}\")\n\n# ── 方式3：stream_events 模式 — 细粒度事件流（含 LLM token） ──\nprint(\"\\n=== stream_events 模式 ===\")\nasync for event in app.astream_events(\n    {\"messages\": [{\"role\": \"user\", \"content\": \"什么是 LangGraph？\"}], \"current_step\": \"\"},\n    config={\"configurable\": {\"thread_id\": \"stream-003\"}},\n    version=\"v2\"\n):\n    if event[\"event\"] == \"on_chain_start\":\n        print(f\"节点开始: {event['name']}\")\n    elif event[\"event\"] == \"on_chain_end\":\n        print(f\"节点完成: {event['name']}, 输出: {event.get('data', {}).get('output', '')[:80]}\")"},{"type":"p","text":"三种 Streaming 方式从粗到细各有用途：stream() 以节点为单位输出，适合展示「正在执行哪一步」的进度条；stream_values() 每步输出完整 State，适合调试和监控状态变化；astream_events() 是最细粒度的事件流，能捕获 LLM 的 token 级输出，适合实现打字机效果的实时回复。在生产项目中，推荐前端用 astream_events 做 token 级展示，后端用 stream 做节点级日志记录。","sub":false,"tts":43},{"type":"flow","steps":[{"kind":"start","label":"START / 入口","edge":"","branch":false},{"kind":"process","label":"Thinking / 思考节点","edge":"","branch":false},{"kind":"decision","label":"Decision / 条件判断","edge":"","branch":false},{"kind":"tool","label":"Search / 搜索节点","edge":"需搜索","branch":false},{"kind":"tool","label":"Parallel / 并行节点","edge":"并行处理","branch":false},{"kind":"decision","label":"Review / 审查节点","edge":"","branch":false},{"kind":"end","label":"END / 出口","edge":"通过","branch":false},{"kind":"process","label":"Answer / 回答节点","edge":"","branch":true}]},{"type":"card","title":"📋 八股总结 — 面试高频考点","accent":"","children":[{"type":"card","title":"","accent":"","children":[{"type":"qaq","text":"Q1: LangGraph 的核心思想是什么？与 LangChain Agent 有什么区别？","tts":44},{"type":"p","text":"核心思想：把 Agent 工作流建模为有向图。节点 = 执行步骤，边 = 跳转逻辑。 区别：LangChain Agent 是黑盒（LLM 自主决定流程），难调试、难干预。LangGraph 是白盒（图结构显式定义），每步可检查 State、支持人工干预、支持并行。 简单场景用 LangChain Agent，复杂生产场景用 LangGraph。","tts":45}],"qa":true},{"type":"card","title":"","accent":"","children":[{"type":"qaq","text":"Q2: LangGraph 的五个核心概念是什么？","tts":46},{"type":"p","text":"① State（状态）：全局共享状态，所有节点可读写。用 TypedDict 定义。 ② Node（节点）：执行单元，一个函数，接收 State 返回更新后的 State。 ③ Edge（边）：节点间连接，普通边=固定跳转。 ④ Conditional Edge（条件边）：根据 State 动态路由，类似 if-else。 ⑤ Checkpoint（检查点）：每步自动保存 State 快照，支持时间旅行、人工干预、错误恢复。","tts":47}],"qa":true},{"type":"card","title":"","accent":"","children":[{"type":"qaq","text":"Q3: LangGraph 如何实现 Human-in-the-Loop？","tts":48},{"type":"p","text":"通过 Checkpoint + interrupt_before/interrupt_after 实现： ① 编译图时指定 interrupt_before=[\"tools\"]，表示在 tools 节点前暂停 ② Agent 执行到暂停点时停止，等待人工操作 ③ 人工可以：直接继续（批准）、修改 State 后继续（纠正）、终止执行（拒绝） ④ 通过 thread_id 关联同一次执行的状态快照，支持从任意点恢复","tts":49}],"qa":true},{"type":"card","title":"","accent":"","children":[{"type":"qaq","text":"Q4: LangGraph 中 State 的 Annotated[list, operator.add] 是什么意思？","tts":50},{"type":"p","text":"Annotated[list, operator.add] 定义了 State 字段的合并策略。 operator.add 表示\"追加\"语义：当节点返回新的 messages 时，新消息追加到已有列表，而不是覆盖。 这对消息历史很重要——每个节点都应该看到完整的对话历史，而不是只看到上一步的结果。","tts":51}],"qa":true},{"type":"card","title":"","accent":"","children":[{"type":"qaq","text":"Q5: LangGraph 如何实现 ReAct 循环？","tts":52},{"type":"p","text":"通过图结构： ① agent 节点（调用 LLM 推理） ② tools 节点（执行工具） ③ 条件边 should_continue：检查最后一条消息有无 tool_calls ④ agent → (条件) → tools → agent → ... 形成循环 ⑤ 当 LLM 不再要求调用工具时，条件边路由到 END，循环结束 ReAct 的 Thought-Action-Observation 循环在 LangGraph 中体现为图中的环（cycle）。","tts":53}],"qa":true},{"type":"card","title":"","accent":"","children":[{"type":"qaq","text":"Q6: LangGraph 中 StateGraph 的核心数据结构是什么？为什么用 TypedDict？","tts":54},{"type":"p","text":"核心数据结构是 StateGraph，它以 TypedDict 作为 State 的类型定义。 使用 TypedDict 的原因有三点：① 类型安全，IDE 可以自动补全字段名和类型，减少拼写错误；② 合并策略声明，通过 Annotated[list, operator.add] 可以指定字段的合并方式（追加 vs 覆盖）；③ 可序列化，TypedDict 本质是 dict，可以直接序列化为 JSON 保存到 Checkpoint 中。 StateGraph 在编译时会根据 TypedDict 的字段定义自动处理节点返回值与现有 State 的合并逻辑，无需手动编写 merge 函数。","tts":55}],"qa":true},{"type":"card","title":"","accent":"","children":[{"type":"qaq","text":"Q7: ConditionEdge 和普通 Edge 的区别是什么？什么场景下用 ConditionEdge？","tts":56},{"type":"p","text":"普通 Edge：固定跳转，A 节点执行完必定跳到 B 节点。语法：graph.add_edge(\"A\", \"B\")。 Conditional Edge：动态路由，根据当前 State 的内容决定下一个节点。语法：graph.add_conditional_edges(\"A\", router_fn, {\"path1\": \"node1\", \"path2\": \"node2\"})。 使用场景：任何需要根据运行时状态做分支的地方都应该用 ConditionEdge。典型场景包括：ReAct 循环中判断是否继续调用工具、多工具选择（根据 LLM 输出决定调用哪个工具）、错误处理（根据错误类型决定重试还是终止）、质量审查（根据评分决定通过还是重试）。","tts":57}],"qa":true},{"type":"card","title":"","accent":"","children":[{"type":"qaq","text":"Q8: Checkpoint 在 LangGraph 中的作用是什么？MemorySaver 和其他 Checkpointer 有什么区别？","tts":58},{"type":"p","text":"Checkpoint 作用：在每个节点执行后自动保存 State 快照，实现三大功能——时间旅行（回到任意历史状态）、人工干预（暂停在指定节点等待人工操作后继续）、错误恢复（从失败点重试而非从头开始）。 MemorySaver：内存级 Checkpointer，数据存在进程内存中，重启后丢失。适合开发调试和单机部署。 SqliteSaver / PostgresSaver：持久化到数据库，重启后数据仍在。适合生产环境，支持多实例共享状态。 选型建议：开发用 MemorySaver，生产用 PostgresSaver。切换只需改一行 compile(checkpointer=xxx)，图定义完全不变。","tts":59}],"qa":true},{"type":"card","title":"","accent":"","children":[{"type":"qaq","text":"Q9: LangGraph 中 Human-in-the-Loop 的三种模式分别是什么？如何实现？","tts":60},{"type":"p","text":"模式1：审批模式。编译时设置 interrupt_before=[\"node_name\"]，在指定节点前自动暂停，等待人工批准后继续。适合高风险操作。 模式2：纠正模式。暂停后通过 app.update_state(config, values={...}) 修改 State 内容，再调用 app.invoke(None, config) 继续。适合 Agent 理解偏差时人工修正。 模式3：对话模式。在条件边中判断是否需要人工输入，跳转到专门的人机交互节点等待用户回答。适合 Agent 信息不足需要补充询问的场景。 三种模式可以组合使用，通过 thread_id 关联同一次执行的状态快照。","tts":61}],"qa":true},{"type":"card","title":"","accent":"","children":[{"type":"qaq","text":"Q10: 项目选型时，LangGraph 和 LangChain Agent 怎么选？给出决策标准。","tts":62},{"type":"p","text":"选 LangChain Agent 的场景：① 快速原型验证，不在乎流程控制；② 简单的单步或少步 Agent，不需要循环和并行；③ 不需要人工干预和状态持久化；④ 团队已经在使用 LangChain 生态，不想引入新依赖。 选 LangGraph 的场景：① 需要精细控制 Agent 每一步的流程；② 需要人工干预（审批、纠正、对话）；③ 需要并行执行多个节点；④ 需要状态持久化和错误恢复；⑤ 生产环境部署，需要可调试性和可观测性；⑥ 复杂工作流，涉及子图、多 Agent 协作。 一句话标准：能用 LangChain Agent 在 10 分钟跑通的简单场景就用它，但凡需要「控制力」就上 LangGraph。","tts":63}],"qa":true},{"type":"card","title":"","accent":"","children":[{"type":"qaq","text":"Q11: LangGraph 的节点和边，和传统工作流有什么区别？","tts":64},{"type":"p","text":"核心区别 = 灵活性 + 循环能力： 传统工作流：节点和跳转边都是固定写死的，一步错就全盘卡壳，也不支持循环执行。适用于流程确定、场景固定的简单业务。 LangGraph： ① 支持条件边——下一个执行节点不是固定的，由大模型的推理结果动态决定。类似 if-else 但决策者是 LLM 而非硬编码逻辑。 ② 支持循环执行——Agent 可以自主反复试错、迭代优化，直到完成任务。这是 Agent 能自主完成复杂任务的核心原因。 一句话话术：传统工作流是确定性流水线，LangGraph是不确定性自主推理链。条件边让 Agent 能\"自主决策\"下一步，循环执行让 Agent 能\"试错优化\"直到完成。","tts":65}],"qa":true}],"qa":false}],"ttsSegs":[{"idx":0,"text":"19.1 LangGraph：把 Agent 流程变成图"},{"idx":1,"text":"LangChain 团队推出的 LangGraph 是 2024 年最火的 Agent 框架之一。核心思想：把 Agent 工作流建模为有向图。"},{"idx":2,"text":"Agent 的每一步是图中的一个节点（Node），步骤之间的跳转是边（Edge）。整个 Agent 就是一张图，可视化、可调试、可控。"},{"idx":3,"text":"19.2 LangGraph 的核心概念"},{"idx":4,"text":"1. State（状态）"},{"idx":5,"text":"全局状态对象，所有节点共享。用 TypedDict 定义，包含消息历史、中间结果等。每个节点可以读写 State。"},{"idx":6,"text":"2. Node（节点）"},{"idx":7,"text":"图中的执行单元。每个节点是一个函数，接收 State，返回更新后的 State。节点 = 一步操作。"},{"idx":8,"text":"3. Edge（边）"},{"idx":9,"text":"节点之间的连接。普通边 = 固定跳转；条件边 = 根据 State 动态决定下一个节点。"},{"idx":10,"text":"4. Conditional Edge（条件边）"},{"idx":11,"text":"根据当前 State 动态路由。类似 if-else：if state['need_search'] → search_node, else → code_node。"},{"idx":12,"text":"5. Checkpoint（检查点）"},{"idx":13,"text":"每个节点执行后自动保存 State 快照。支持时间旅行（回到任意检查点）、人工干预（暂停在某节点等人审核后继续）、错误恢复（从失败点重试）。"},{"idx":14,"text":"19.3 用 LangGraph 构建 ReAct Agent"},{"idx":15,"text":"代码解析"},{"idx":16,"text":"State 定义"},{"idx":17,"text":"Annotated[list, operator.add] 表示 messages 列表用\"追加\"而非\"覆盖\"语义。新消息追加到列表，不替换旧消息。"},{"idx":18,"text":"条件边"},{"idx":19,"text":"should_continue 函数检查最后一条消息是否包含 tool_calls。有则路由到 tools 节点，无则结束。"},{"idx":20,"text":"循环结构"},{"idx":21,"text":"agent → (条件) → tools → agent → ... 形成循环。这就是 ReAct 的循环在 LangGraph 中的表达。"},{"idx":22,"text":"Checkpointer"},{"idx":23,"text":"MemorySaver() 保存每步状态。通过 thread_id 可以回到任意检查点，支持人工干预。"},{"idx":24,"text":"19.4 Human-in-the-Loop：人工干预"},{"idx":25,"text":"LangGraph 的 Checkpoint 机制让\"人工干预\"变得自然："},{"idx":26,"text":"19.5 LangGraph vs LangChain Agent"},{"idx":27,"text":"19.6 LangGraph 完整实战代码"},{"idx":28,"text":"前面几节我们分别学习了 State、Node、Edge 和 Checkpoint 的概念，现在把它们组合起来，写一个完整可运行的 LangGraph 项目。这个示例模拟一个智能客服 Agent：接收用户问题，判断是否需要查数据库、调用外部 API 或直接回答，执行后在审查节点检查结果质量，不达标则重试，达标则返回最终回复。"},{"idx":29,"text":"上面这段代码展示了 LangGraph 的完整开发流程：定义 State → 编写节点函数 → 定义条件路由 → 组装图 → 编译运行。几个关键设计点值得注意：第一，step_count 字段用于防止无限循环，当步数超过 5 时强制结束，这是生产环境中的必备安全措施。第二，review 节点充当质量守门员，不满意的回复会被打回 agent 节点重试，形成了一个自我纠错的闭环。第三，MemorySaver 配合 thread_id 实现了状态持久化，同一个 thread 的多次调用可以无缝续接，这对于多轮对话场景至关重要。"},{"idx":30,"text":"在实际项目中，你只需要把 agent_node 中的伪逻辑替换为真实的 LLM 调用，把 search_db_node 替换为你的数据库查询逻辑，就可以直接部署使用了。整个图结构的定义不需要改动，这就是 LangGraph 「一次建模、反复使用」的优势所在。"},{"idx":31,"text":"19.7 LangGraph vs 其他框架对比"},{"idx":32,"text":"除了 LangGraph，市面上还有多个流行的 Agent 框架，它们各有侧重。理解差异有助于在项目中做出正确的技术选型。下面从五个关键维度对比四大主流框架。"},{"idx":33,"text":"从对比可以看出，LangGraph 在图结构表达力和状态可控性方面优势明显：它既支持循环（ReAct 模式）又支持并行（多节点同时触发），加上 Checkpoint 机制让 Human-in-the-Loop 变得开箱即用。AutoGen 更适合多 Agent 自由对话的研究场景，CrewAI 则擅长按预设角色分工的任务流水线。选型建议：需要精细控制流程选 LangGraph，快速验证选 LangChain，多 Agent 协作选 AutoGen，角色分工明确选 CrewAI。"},{"idx":34,"text":"19.8 高级特性：子图与人机协同"},{"idx":35,"text":"子图（Subgraph）：模块化管理复杂流程"},{"idx":36,"text":"当 Agent 工作流变得复杂时，把所有节点放在一个图里会导致可读性和可维护性下降。子图允许你把一组相关节点封装成一个独立子图，然后作为单个节点嵌入主图。这样做有三个好处：一是逻辑分层，主图只关心高层流程，子图处理细节；二是复用性，同一个子图可以在不同主图中重复使用；三是独立测试，子图可以单独编译和调试，不影响主图。"},{"idx":37,"text":"这段代码展示了两层图结构：内层 research_subgraph 负责「生成查询 → 执行搜索 → 汇总结果」三步，外层主图调用子图后将结果传给 write_report 节点生成最终报告。子图的 State（ResearchState）与主图的 State（MainState）完全独立，通过 research_node 函数做桥接转换，这种设计让两层的逻辑互不污染。"},{"idx":38,"text":"Human-in-the-Loop：三种人机协同模式"},{"idx":39,"text":"LangGraph 的人机协同不止「暂停-审核-继续」一种模式，实际项目中有三种常见模式：审批模式（执行前等人批准）、纠正模式（暂停后人工修改 State 再继续）、对话模式（Agent 遇到不确定时主动向人提问）。下面用代码演示这三种模式的实现。"},{"idx":40,"text":"三种模式各有适用场景：审批模式适合高风险操作（如删除数据、发送邮件），纠正模式适合 Agent 理解有偏差时人工修正，对话模式适合 Agent 信息不足需要向人补充询问。生产项目中可以组合使用，比如先对话模式收集信息，再审批模式确认执行。"},{"idx":41,"text":"Streaming：流式输出实时反馈"},{"idx":42,"text":"用户体验角度，长时间运行的 Agent 必须提供实时反馈，否则用户会以为程序卡住了。LangGraph 原生支持 Streaming，可以流式输出每个节点的结果、State 变化以及 LLM 的 token 级流式输出。"},{"idx":43,"text":"三种 Streaming 方式从粗到细各有用途：stream() 以节点为单位输出，适合展示「正在执行哪一步」的进度条；stream_values() 每步输出完整 State，适合调试和监控状态变化；astream_events() 是最细粒度的事件流，能捕获 LLM 的 token 级输出，适合实现打字机效果的实时回复。在生产项目中，推荐前端用 astream_events 做 token 级展示，后端用 stream 做节点级日志记录。"},{"idx":44,"text":"Q1: LangGraph 的核心思想是什么？与 LangChain Agent 有什么区别？"},{"idx":45,"text":"核心思想：把 Agent 工作流建模为有向图。节点 = 执行步骤，边 = 跳转逻辑。 区别：LangChain Agent 是黑盒（LLM 自主决定流程），难调试、难干预。LangGraph 是白盒（图结构显式定义），每步可检查 State、支持人工干预、支持并行。 简单场景用 LangChain Agent，复杂生产场景用 LangGraph。"},{"idx":46,"text":"Q2: LangGraph 的五个核心概念是什么？"},{"idx":47,"text":"① State（状态）：全局共享状态，所有节点可读写。用 TypedDict 定义。 ② Node（节点）：执行单元，一个函数，接收 State 返回更新后的 State。 ③ Edge（边）：节点间连接，普通边=固定跳转。 ④ Conditional Edge（条件边）：根据 State 动态路由，类似 if-else。 ⑤ Checkpoint（检查点）：每步自动保存 State 快照，支持时间旅行、人工干预、错误恢复。"},{"idx":48,"text":"Q3: LangGraph 如何实现 Human-in-the-Loop？"},{"idx":49,"text":"通过 Checkpoint + interrupt_before/interrupt_after 实现： ① 编译图时指定 interrupt_before=[\"tools\"]，表示在 tools 节点前暂停 ② Agent 执行到暂停点时停止，等待人工操作 ③ 人工可以：直接继续（批准）、修改 State 后继续（纠正）、终止执行（拒绝） ④ 通过 thread_id 关联同一次执行的状态快照，支持从任意点恢复"},{"idx":50,"text":"Q4: LangGraph 中 State 的 Annotated[list, operator.add] 是什么意思？"},{"idx":51,"text":"Annotated[list, operator.add] 定义了 State 字段的合并策略。 operator.add 表示\"追加\"语义：当节点返回新的 messages 时，新消息追加到已有列表，而不是覆盖。 这对消息历史很重要——每个节点都应该看到完整的对话历史，而不是只看到上一步的结果。"},{"idx":52,"text":"Q5: LangGraph 如何实现 ReAct 循环？"},{"idx":53,"text":"通过图结构： ① agent 节点（调用 LLM 推理） ② tools 节点（执行工具） ③ 条件边 should_continue：检查最后一条消息有无 tool_calls ④ agent → (条件) → tools → agent → ... 形成循环 ⑤ 当 LLM 不再要求调用工具时，条件边路由到 END，循环结束 ReAct 的 Thought-Action-Observation 循环在 LangGraph 中体现为图中的环（cycle）。"},{"idx":54,"text":"Q6: LangGraph 中 StateGraph 的核心数据结构是什么？为什么用 TypedDict？"},{"idx":55,"text":"核心数据结构是 StateGraph，它以 TypedDict 作为 State 的类型定义。 使用 TypedDict 的原因有三点：① 类型安全，IDE 可以自动补全字段名和类型，减少拼写错误；② 合并策略声明，通过 Annotated[list, operator.add] 可以指定字段的合并方式（追加 vs 覆盖）；③ 可序列化，TypedDict 本质是 dict，可以直接序列化为 JSON 保存到 Checkpoint 中。 StateGraph 在编译时会根据 TypedDict 的字段定义自动处理节点返回值与现有 State 的合并逻辑，无需手动编写 merge 函数。"},{"idx":56,"text":"Q7: ConditionEdge 和普通 Edge 的区别是什么？什么场景下用 ConditionEdge？"},{"idx":57,"text":"普通 Edge：固定跳转，A 节点执行完必定跳到 B 节点。语法：graph.add_edge(\"A\", \"B\")。 Conditional Edge：动态路由，根据当前 State 的内容决定下一个节点。语法：graph.add_conditional_edges(\"A\", router_fn, {\"path1\": \"node1\", \"path2\": \"node2\"})。 使用场景：任何需要根据运行时状态做分支的地方都应该用 ConditionEdge。典型场景包括：ReAct 循环中判断是否继续调用工具、多工具选择（根据 LLM 输出决定调用哪个工具）、错误处理（根据错误类型决定重试还是终止）、质量审查（根据评分决定通过还是重试）。"},{"idx":58,"text":"Q8: Checkpoint 在 LangGraph 中的作用是什么？MemorySaver 和其他 Checkpointer 有什么区别？"},{"idx":59,"text":"Checkpoint 作用：在每个节点执行后自动保存 State 快照，实现三大功能——时间旅行（回到任意历史状态）、人工干预（暂停在指定节点等待人工操作后继续）、错误恢复（从失败点重试而非从头开始）。 MemorySaver：内存级 Checkpointer，数据存在进程内存中，重启后丢失。适合开发调试和单机部署。 SqliteSaver / PostgresSaver：持久化到数据库，重启后数据仍在。适合生产环境，支持多实例共享状态。 选型建议：开发用 MemorySaver，生产用 PostgresSaver。切换只需改一行 compile(checkpointer=xxx)，图定义完全不变。"},{"idx":60,"text":"Q9: LangGraph 中 Human-in-the-Loop 的三种模式分别是什么？如何实现？"},{"idx":61,"text":"模式1：审批模式。编译时设置 interrupt_before=[\"node_name\"]，在指定节点前自动暂停，等待人工批准后继续。适合高风险操作。 模式2：纠正模式。暂停后通过 app.update_state(config, values={...}) 修改 State 内容，再调用 app.invoke(None, config) 继续。适合 Agent 理解偏差时人工修正。 模式3：对话模式。在条件边中判断是否需要人工输入，跳转到专门的人机交互节点等待用户回答。适合 Agent 信息不足需要补充询问的场景。 三种模式可以组合使用，通过 thread_id 关联同一次执行的状态快照。"},{"idx":62,"text":"Q10: 项目选型时，LangGraph 和 LangChain Agent 怎么选？给出决策标准。"},{"idx":63,"text":"选 LangChain Agent 的场景：① 快速原型验证，不在乎流程控制；② 简单的单步或少步 Agent，不需要循环和并行；③ 不需要人工干预和状态持久化；④ 团队已经在使用 LangChain 生态，不想引入新依赖。 选 LangGraph 的场景：① 需要精细控制 Agent 每一步的流程；② 需要人工干预（审批、纠正、对话）；③ 需要并行执行多个节点；④ 需要状态持久化和错误恢复；⑤ 生产环境部署，需要可调试性和可观测性；⑥ 复杂工作流，涉及子图、多 Agent 协作。 一句话标准：能用 LangChain Agent 在 10 分钟跑通的简单场景就用它，但凡需要「控制力」就上 LangGraph。"},{"idx":64,"text":"Q11: LangGraph 的节点和边，和传统工作流有什么区别？"},{"idx":65,"text":"核心区别 = 灵活性 + 循环能力： 传统工作流：节点和跳转边都是固定写死的，一步错就全盘卡壳，也不支持循环执行。适用于流程确定、场景固定的简单业务。 LangGraph： ① 支持条件边——下一个执行节点不是固定的，由大模型的推理结果动态决定。类似 if-else 但决策者是 LLM 而非硬编码逻辑。 ② 支持循环执行——Agent 可以自主反复试错、迭代优化，直到完成任务。这是 Agent 能自主完成复杂任务的核心原因。 一句话话术：传统工作流是确定性流水线，LangGraph是不确定性自主推理链。条件边让 Agent 能\"自主决策\"下一步，循环执行让 Agent 能\"试错优化\"直到完成。"}],"toc":[{"level":2,"text":"19.1 LangGraph：把 Agent 流程变成图","anchor":"h-0"},{"level":2,"text":"19.2 LangGraph 的核心概念","anchor":"h-1"},{"level":2,"text":"19.3 用 LangGraph 构建 ReAct Agent","anchor":"h-2"},{"level":3,"text":"代码解析","anchor":"h-3"},{"level":2,"text":"19.4 Human-in-the-Loop：人工干预","anchor":"h-4"},{"level":2,"text":"19.5 LangGraph vs LangChain Agent","anchor":"h-5"},{"level":2,"text":"19.6 LangGraph 完整实战代码","anchor":"h-6"},{"level":2,"text":"19.7 LangGraph vs 其他框架对比","anchor":"h-7"},{"level":2,"text":"19.8 高级特性：子图与人机协同","anchor":"h-8"},{"level":3,"text":"子图（Subgraph）：模块化管理复杂流程","anchor":"h-9"},{"level":3,"text":"Human-in-the-Loop：三种人机协同模式","anchor":"h-10"},{"level":3,"text":"Streaming：流式输出实时反馈","anchor":"h-11"}]};
