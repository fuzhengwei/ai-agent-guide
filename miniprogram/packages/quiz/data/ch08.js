module.exports = [{"id":"ch08_q01","type":"single","question":"Agent Loop 的六步执行流程顺序是？","options":["Perceive → Act → Reason → Plan → Observe → Evaluate","Perceive → Reason → Plan → Act → Observe → Evaluate","Reason → Plan → Act → Observe → Evaluate → Perceive","Plan → Perceive → Reason → Act → Observe → Evaluate"],"answer":[1],"explanation":"标准 Agent Loop 六步流程为：感知(Perceive) → 推理(Reason) → 规划(Plan) → 执行(Act) → 观察(Observe) → 评估(Evaluate)。这是一个事件驱动的循环，每轮工具调用结果触发下一轮推理。"},{"id":"ch08_q02","type":"single","question":"Loop、Runtime、Sandbox 三者的嵌套关系是？","options":["Loop 在最外层，Runtime 在中间层，Sandbox 在最内层","Runtime 在最外层，Sandbox 在中间层，Loop 在最内层","Sandbox 在最外层，Runtime 在中间层，Loop 在最内层","三者并列，无嵌套关系"],"answer":[2],"explanation":"Sandbox 在最外层（所有 Agent 行为最终都在某个隔离环境中执行），Runtime 在中间层（为 Loop 提供基础设施），Agent Loop 在最内层（是 Runtime 中的逻辑循环）。"},{"id":"ch08_q03","type":"single","question":"Firecracker 微虚拟机与 Docker 容器的主要区别是？","options":["Firecracker 更轻量，启动更快","Docker 提供内核级隔离，Firecracker 只隔离进程","Firecracker 每个沙箱运行独立内核，Docker 共享宿主机内核","两者隔离级别相同，只是实现方式不同"],"answer":[2],"explanation":"Docker 容器共享宿主机内核，隔离在进程和文件系统层面。Firecracker 每个微虚拟机运行独立内核，提供内核级隔离，逃逸难度极高。"},{"id":"ch08_q04","type":"multi","question":"Agent Loop 的常见设计陷阱有哪些？（多选）","options":["死循环：工具永远失败但 LLM 一直重试","Token 爆炸：每轮把全部历史塞入 Context Window","幻觉工具调用：LLM 调用不存在或参数错误的工具","并行过多：同时调用太多工具导致超时"],"answer":[0,1,2],"explanation":"三大陷阱：① 死循环——工具连续失败但Agent不停重试；② Token 爆炸——每轮重复全部历史；③ 幻觉工具调用——LLM 调用不存在的工具或传错参数。"},{"id":"ch08_q05","type":"single","question":"Runtime 的五层架构从底层到上层依次是？","options":["可观测层 → 执行调度层 → 记忆管理层 → 工具注册层 → LLM管理层","LLM管理层 → 工具注册层 → 记忆管理层 → 执行调度层 → 可观测层","执行调度层 → LLM管理层 → 记忆管理层 → 工具注册层 → 可观测层","工具注册层 → LLM管理层 → 可观测层 → 记忆管理层 → 执行调度层"],"answer":[1],"explanation":"Runtime 五层架构：① LLM管理层（模型路由、Token管理、重试降级）；② 工具注册层（定义、校验、权限）；③ 记忆管理层（短期/长期/工作记忆）；④ 执行调度层（Loop引擎、并发、超时熔断）；⑤ 可观测层（Tracing、Metrics、Logging）。"},{"id":"ch08_q06","type":"single","question":"Agent Loop 中 max_turns 参数的主要作用是？","options":["限制每轮工具调用的最大次数","限制 Agent 循环的最大迭代轮数","限制 LLM 的最大 Token 输出","限制 Agent 的最大运行时间"],"answer":[1],"explanation":"max_turns 限制 Agent 循环的最大迭代轮数（如设为 20，则 Agent 最多循环 20 轮后强制停止）。这是防止死循环和成本失控的硬性兜底措施，与超时限制（timeout）配合使用。"},{"id":"ch08_q07","type":"single","question":"Runtime 层的核心职责不包括以下哪项？","options":["管理 Agent 生命周期（启动、暂停、恢复、终止）","提供工具执行环境（文件系统、进程、网络）","执行 LLM 推理计算（前向传播）","资源配额管理（CPU、内存、时间限制）"],"answer":[2],"explanation":"Runtime 层负责 Agent 生命周期管理、工具执行环境和资源配额，但不执行 LLM 推理计算。LLM 推理由模型服务端完成，Runtime 只负责调用 API 并处理响应。"},{"id":"ch08_q08","type":"single","question":"WaLiCode 的五层权限递进系统中，第一层是？","options":["规则引擎匹配（正则 + 关键词）","Bash AST 分析（解析命令结构）","AI 安全审判（LLM 判断风险）","用户审批（Human-in-the-Loop）"],"answer":[1],"explanation":"五层权限递进为：① Bash AST 分析 → ② 规则引擎匹配 → ③ AI 安全审判 → ④ 用户审批 → ⑤ 熔断器。从自动到人工，从快速到审慎，分层递进。"},{"id":"ch08_q09","type":"multi","question":"沙箱隔离的常见实现方式有哪些？（多选）","options":["Docker 容器隔离（进程+文件系统级）","Firecracker 微虚拟机（内核级隔离）","WASM 沙箱（内存安全+快速启动）","gVisor 系统调用拦截（内核接口级）"],"answer":[0,1,2,3],"explanation":"四种主流沙箱方案：Docker（进程级）、Firecracker（微VM，独立内核）、WASM（内存安全，毫秒级启动）、gVisor（拦截系统调用）。隔离级别从低到高：Docker < gVisor < WASM < Firecracker。"},{"id":"ch08_q10","type":"single","question":"Agent Loop 中'滑动窗口压缩'策略的核心思想是？","options":["只保留最近 N 轮对话历史，更早的丢弃","只保留最近 N 轮对话，更早的用摘要替代","只保留工具调用结果，丢弃对话内容","把所有历史压缩成单个摘要"],"answer":[1],"explanation":"滑动窗口压缩：保留最近 N 轮完整对话（保证近期上下文不丢失），更早的历史用摘要替代（保留关键信息但大幅减少 Token）。这是 Token 爆炸问题的标准解决方案。"},{"id":"ch08_q11","type":"single","question":"以下哪个不是 Loop Engineering 的核心原则？","options":["事件驱动：工具结果触发下一轮推理，而非固定间隔","可中断：用户随时可以暂停或终止 Agent 循环","无状态：每轮循环独立，不依赖前轮结果","可观测：每一步都有日志和 trace，可回放调试"],"answer":[2],"explanation":"Loop Engineering 的核心原则包括事件驱动、可中断、可观测，但不包括'无状态'。Agent Loop 是有状态的——每轮循环依赖前轮结果，状态在轮次间传递。无状态设计会破坏 Agent 的连贯推理能力。"}];
