# 2026-09-24

## 划线收藏 + 邮箱登录（云服务）
- 为静态站接入 WorkBuddy 云服务：applicationId `wbapp_62qsE5HMRFHmZdl8SnVl74`（AI Agent 教程），endpoint `https://ai-agent-guide-66900.app.workbuddy.host`
- 云表 `user_notes`：owner_id（DEFAULT auth.uid()）/ chapter_id / chapter_title / note_type(highlight|favorite) / selected_text / note_text / color；RLS 全开，owner-only 读写，索引 idx_user_notes_owner_chapter
- 新增 `js/notes.js`（UserNotes 模块）：CDN 加载 workbuddy-cloud-sdk（@dev），邮箱密码登录/OTP 登录/注册/忘记密码；选中正文文字弹浮动工具条（划线/收藏/笔记）；mark.user-note-mark 渲染与点击气泡（编辑/删除）；右侧 📒 笔记面板按章节分组；hook App.loadChapter 切换章节后恢复划线
- index.html 引入 SDK CDN + js/notes.js
- ⚠️ Auth 只在已发布的注册域名可用（localhost/预览不支持），需发布后才能验证登录链路；发布时用 workbuddy_sites_deploy 并复用上述 appId

## 排行榜 + 学习时长云同步（同日追加）
- 新增云表：`user_profiles`（owner_id PK / nickname / total_study_ms / completed_chapters，owner-only 读写）与 `exam_records`（每次考试成绩，owner-only 读写）
- RPC `get_leaderboard(board text, lim int)`：SECURITY DEFINER，只返回脱敏昵称+数值（study 榜=时长 ms、exam 榜=最高分），GRANT EXECUTE 给 authenticated,anon——**排行榜只经此函数读，表本身不允许跨人 SELECT**
- 新增 `js/leaderboard.js`：右侧 🏆 按钮 + 排行面板（时长榜/高分榜双 tab）、昵称设置（默认邮箱前缀脱敏）、hook Progress.saveQuizResult/saveExamScore 自动上报成绩、每 60s 检查同步时长（增量 ≥2min 才上传以省资源点）
- 复用 UserNotes.cloud 实例与登录态，未登录看榜提示登录；index.html 已引入

## 小程序云开发（方案A，同日追加）
- 用户要的是**小程序版**收藏/登录/排行榜，采用微信云开发（非 WorkBuddy 云服务——后者只认网页 Origin，小程序用不了）
- project.config.json 加 `cloudfunctionRoot: cloudfunctions/`；app.js 初始化 `wx.cloud.init` + 静默 login（openid 无感登录）
- 云函数 3 个（`miniprogram/cloudfunctions/`）：login（档案+昵称）、notes（收藏 CRUD，user_notes 集合）、leaderboard（report 上报 + list 榜单，只回昵称不回 openid）
- 前端：`utils/cloud.js` 封装（未开通云开发时静默降级）；store.js 加 STUDY_TIME 时长统计
- reader 模板（mp-templates/reader/，已同步 4 分包）：rich-text 加 selectable；悬浮 ☆ 按钮取剪贴板选段 → 弹窗写笔记收藏；15s 计时、每 2min 上报
- 新页面：pages/notes（我的收藏）、pages/leaderboard（时长榜/积分榜 tab + 设昵称）；mine 页加学习时长卡 + 两个入口；app.json 已注册
- ⚠️ 需在开发者工具里手动：开通云开发 → 部署 3 个云函数 → 建集合 user_notes / user_profiles（权限"仅创建者可读写"）
- ⚠️ 收藏交互受 rich-text 限制：流程是「长按选中→复制→点☆」，不能做到网页那种选中即弹工具条

## 小程序阅读体验修复（6 项）
- 代码块横滑：rich-text 加 `catchtouchmove="onRichTouch"` 阻止横滑冒泡成页面滚动；mp-codeblock 重构为 mac 风卡（三色点头部 mp-code-head + 语言标签 + 「‹ 左右滑动 ›」提示 + ::after 行尾渐隐）
- 表格线条修复：display:block 下 border-collapse 失效 → 改 border-spacing:0 + 单元格只画右/下边框拼网格，显式边框色（日间 #dde3ee / 夜间 #2a303d）
- 面试自动滚动：runner.js `_bottom()` 改为先清空 scrollInto、setTimeout 60ms 后再设新锚点（内容未铺好时立即 setData 会被 scroll-view 忽略）
- 章节目录：构建脚本 renderNode/renderFlow/renderContainer 全链路加第 4 参 toc，h2/h3 生成锚点 id="h-N" 并收集 {level,text,anchor} 写入章节数据；reader 右侧 ☰ 抽屉（scroll-into-view 回顶），tapTocItem 用 SelectorQuery boundingClientRect+scrollOffset 算绝对位置 pageScrollTo
- 阅读位置记忆：store.js 新增 saveReadPos/getReadPos/setLastChapter/getLastChapter（dsh_read_pos / dsh_last_chapter）；reader onPageScroll 200ms 节流落盘、onLoad 后 350ms 恢复（top<300 不打扰）并弹 resume-tip；index.js onShow 一次性 showModal「继续上次阅读」
- 构建验证：31 章含 toc、锚点 id、code-hint 均注入；4 个内容分包模板已更新；总体积 2.79MB

## 小程序本地预览页
- `tools/generate-preview.js` 重写：数据直接 require 构建产物 .js 模块；样式编译 `reader.wxss`（rpx→px/2、变量展开、`.reader-wrap→.phone-screen-reader`、`.rich→.rich-html`）→ 预览与真机 1:1
- 关键预览适配：`.phone { contain: layout }` 让 fixed 元素（目录抽屉/悬浮按钮/遮罩）相对手机框定位，模拟小程序屏幕；不要用 absolute（会随内容滚走）
- 三机并列：首页「继续上次阅读」弹窗 / 阅读页（代码块横滑+☰目录+⟳模拟重进恢复位置）/ 面试自动滚动演示；演示章 ch07-mcp（35 条目录）
- 本地服务：`python3 -m http.server 8899 --directory <root>` 需 run_in_background（nohup & 会被回收）；⚠️ 环境有 HTTP_PROXY=127.0.0.1:50837，agent-browser/curl 访问 localhost 须先 `export NO_PROXY=127.0.0.1,localhost` + unset 代理变量，否则 ERR_CONNECTION_REFUSED
- agent-browser：eval 执行 JS、click 只对视口内元素有效；截图存 .workbuddy/preview-shots/



## 小程序全页面分享/转发（2026-09-24）
- 新增 miniprogram/utils/share.js：`share.attach(title, path, extra)` 一次性注入 onShareAppMessage + onShareTimeline（朋友圈入口需页面定义后者才可用）；title/path 支持函数（this=页面实例）做动态文案
- 接入 8 个页面：首页/题库大厅/我的（主包，直改）+ 4 个 reader 分包 + quiz runner（改 mp-templates 后重跑构建）
- 动态文案：reader 分享当前章节路径、runner 面完带成绩文案、mine 带已读进度
- UI：阅读页悬浮工具加 `button open-type=share` 圆形按钮（.tool-btn.share-btn 清 button 默认样式 + ::after 去边框）；runner 报告页加「分享我的战绩」；首页 hero 加分享条；mine 菜单加「推荐给朋友」
- 注意：页面里分享按钮必须用 button[open-type=share]，view bindtap 无法触发转发面板

## 网页端阅读工具三件套（2026-09-24）
- 新增 `js/reader.js`（Reader 模块）+ `js/progress-plus.js`（ProgressPlus 模块），index.html 引入（v=20260924.1），样式追加在 book.css 末尾
- 字号调整：顶栏 Aa 按钮 + 弹层，80%~160% 步进 10%，CSS 变量 `--reader-scale` 只缩放正文（p/li/blockquote/h1-h3/qa），表格代码不受影响；localStorage key `ai-agent-guide-reader-scale`
- 语音朗读：Web Speech API（纯前端免费），音色下拉（优先 zh 音色，_niceName 映射友好名）+ 语速/音调滑杆；collectSegments 收集可见 tab 正文（跳过代码/考试/隐藏 tab，长段按句拆 160 字）；朗读段落 .tts-reading 高亮+scrollIntoView 跟随；切换章节 Reader.onChapterChange() 自动停止
- 进度增强：内容区顶部阅读进度条（scroll 节流记录 scrollPct）；累计学习时长（15s 结算一次、30s 无活动/页面隐藏不计时，存 days/totalTime）；侧边栏时长徽章；重新进章弹「上次读到 X%」恢复条（<8% 或 >92% 不打扰）；封面注入「继续学习」卡片（上次章节+时长+已学章数）
- 数据存 localStorage `ai-agent-guide-progress-plus`；App.init/loadChapter/goHome 已挂钩子
- ⚠️ ProgressPlus.data 是 init 时的内存对象，测试改 localStorage 后需刷新或直接改内存对象才生效

## 小程序端阅读工具三件套（2026-09-24）
- 字号：reader 从 3 档(s/m/l)升级为连续步进（22~38rpx，步进2），pref 兼容旧 fontMode；样式改用 `--fs` CSS 变量挂在 reader-wrap style 上
- 语音朗读：新增 `utils/tts.js` 封装 wechatsi 插件（textToSpeech 逐段合成 + InnerAudioContext 播放队列，VOICES 3 种音色用 rateFactor 模拟）；构建脚本 `injectTtsSpans` 为 p/li/sh/quote/qa 注入 `data-tts-idx`（ch00 有 102 段）；reader 收集段落在 `_collectSegments` 用正则从 html 字符串提取文本，SelectorQuery 按 idx 滚动跟随
- ⚠️ **wechatsi 插件（appid wx069ba97219f66d99）已从插件市场下架**（2024 后不再接受新主体添加），改用**云函数 TTS 方案**：`cloudfunctions/tts/index.js` 调腾讯云 TTS TextToVoice（voiceType 101001/101002/101003），utils/tts.js 用 wx.cloud.callFunction 合成 + InnerAudioContext 播放临时文件
- 云函数 tts 部署前提：云开发控制台上传部署 + 环境变量配置 TENCENT_SECRET_ID/TENCENT_SECRET_KEY（腾讯云 API 密钥管理创建，TTS 需开通）
- 学习进度：saveReadPos 增加 pct 参数；首页章节列表显示阅读进度条+百分比，继续学习卡显示上次位置和累计时长（studyTimeText）
- ⚠️ 构建后 miniprogram/ 4 个内容分包的 reader.* 与 tools/mp-templates/reader/ 保持同步（已验证一致）
- 上传：ci/upload.js 20260924.2 已上传开发版（robot 1），总 2.98MB

## 勇者冒险岛 · 答题闯关玩法（2026-09-24）
- 题库 tab（pages/quiz/quiz）从模拟面试列表改为竖版卡通闯关地图：28 关沿贝塞尔山路蜿蜒至雪巅王冠，7 个区域色带+云朵+路边装饰
- 关卡数据在 miniprogram/data/adventure.js：LEVELS 的 quizKey 与章节解锁链顺序一致（用 build 脚本 registry 同款），地图坐标 100x1700，节点 y 严格递减
- 闯关答题页为 packages/quiz/pages/adventure/adventure，源码在 tools/mp-templates/quiz/adventure.*，由 build-miniprogram.js 拷贝并注册进 quiz 分包
- 规则：每关随机抽 10 题（选项乱序），全对通关解锁下一关（进度存 dsh_adventure），错一题留在本关重抽；已通关关卡为自由练习
- 关卡记录存 dsh_quiz_records 用 adv_ 前缀隔离，不影响模拟面试统计；宝箱/王冠彩蛋、通关烟花 CSS 粒子
- 预览页：adventure-preview.html

## 分享卡片小图（2026-09-24）
- `tools/make_share_card.py`（Pillow）绘制 500x400 分享图 → `miniprogram/assets/share-card.png`；深色渐变底+金色顶线+胶囊卖点+右下 site-logo，字体用 `/System/Library/Fonts/Hiragino Sans GB.ttc`（index 0=W3 / 2=W6，本机无 PingFang.ttc）
- `utils/share.js` 的 SHARE_IMAGE 已指向 `/assets/share-card.png`，全页面分享卡片生效；改文案重跑该脚本即可

## 阅读页三 bug 修复：正文渲染从 rich-text 改为节点树（2026-09-24 晚）
- 背景：rich-text + catchtouchmove 吃掉整页竖滑；CSS 变量不传入 rich-text 内部节点导致字号调节无效；rich-text 丢弃节点 id 导致目录无法定位
- 方案：build-miniprogram.js 输出由 HTML 字符串改为结构化节点树（chapter data 里 html → nodes + ttsSegs），reader.wxml 用 wx:for/递归 block 渲染 nd-* 节点；代码块/表格用独立 scroll-view 横滑
- 动画块转换（convertCompare/Flowchart/Step）返回节点对象，经 nodeToPlaceholder（div.mp-json data-json）占位后由 renderNode 还原
- 字号：.fs-c 容器注入 --fs，正文类用「WXML固定字号 + .fs-c 复合选择器覆盖」双轨实现缩放；h2/h3 锚点成为真实 view id，目录点击可定位
- 朗读：assignTts/flattenTts 在构建期分配序号并产出 ttsSegs，reader.js 直接用，不再正则解析 HTML
- 校验：全部 28 章 nodes 非空、toc 锚点与节点一一对应、tts 计数一致、232 个表格节点正常
