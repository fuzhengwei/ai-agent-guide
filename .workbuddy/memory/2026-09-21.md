# 2026-09-21

## 微信小程序版本开发（ai-agent-guide.xiaofuge.cn）
- 用户要求把 AI Agent 教程网站做成微信小程序并上架，助手全权负责开发，用户只提供注册信息。
- 搭建原生小程序工程 miniprogram/：主包（课程/题库/我的 三 Tab）+ 5 个分包（content-a~d 章节内容 + quiz 题库），全内容内嵌、无外部接口依赖，主包仅 21KB。
- 新增 tools/build-miniprogram.js：cheerio 解析 chapters/*.html → rich-text 友好 JSON；动画（flowchart/compare/step）转静态卡片；题库按章拆分；按标题「第N章」字号排序章节；生成各分包静态 require 索引 index.js。
- 踩坑：cheerio 对象用 .each 非 .forEach；转换后的 mp-* 块会被通用 div 分支拍平（加直通分支）；小程序 require 不支持完全动态路径；quiz 键 chNN 与注册表 cNN 需按章节号映射（registry 增加 num/quizKey 字段）。
- tools/generate-preview.js 生成 miniprogram-preview.html 手机框 UI 预览（内嵌真实数据）。
- docs/小程序上架清单.md：账号注册→AppID→上传密钥→备案→类目→发布流程（miniprogram-ci 代上传，用户扫码提交审核）。
- 待办：等用户提供 AppID 与上传密钥；上传前需真机/开发者工具验证 UI 细节。

## 主体决策补充
- 用户有个体工商户执照，倾向企业主体注册小程序以支持微信支付（付费解锁/卖课）。
- 已更新 docs/小程序上架清单.md：方案A 个体户主体（可支付，认证300元/年，商户号可绑经营者个人卡）vs 方案B 个人主体（仅流量主广告）。
- 注意点：个体户类目走「教育-在线教育（知识付费）」，避免定位成"题库/教辅工具"（部分三级类目仅对企业开放）；ICP备案主体需与执照一致。
- 最终确定走个体工商户主体。docs/小程序上架清单.md 重写为 7 步最终版（准备材料→注册→认证300元→AppID+上传密钥→备案→类目教育-在线教育→微信支付商户号→提审发布）。用户需提供：AppID、private.key、mch_id/APIv3密钥（支付阶段）。
## 小程序题库映射修复（用户已具备开发者身份）
- 校验小程序完整性：28 章 / 28 套题库 / 8 页面 / 主包 22.5KB，结构正常
- **发现重大问题**：chapters.json 的 quizKey 与题库文件内容大面积错位（如 ch05.json 全是 ReAct 题却映射到"第5章上下文工程"），已逐一按主题核对并修正 23 个映射
- **补写缺失题库**：新建 ch26.json（第5章 上下文工程 15题）、ch27.json（第12章 LLM-Wiki 15题），注册到 packages/quiz/data/index.js 和 data/quiz-index.json
- 修改文件：data/chapters.json、data/quiz-index.json、packages/quiz/data/index.js、packages/quiz/data/ch26.json（新）、ch27.json（新）
- Node 实际加载验证：28/28 章节绑定成功，共 463 题，题目结构全部合法

## 付费解锁框架 + 上传流水线
- 新增 utils/pay.js：买断制解锁（前8章免费），PAY_ENABLED 开关；index/reader(×4分包)/mine 全部接入锁定守卫与解锁入口
- 根治映射错位：构建脚本加入 QUIZ_KEY_BY_SLUG 显式映射；ch26/ch27 题库合并进源 data/quiz-bank.json；reader 付费守卫同步进 tools/mp-templates
- 重跑构建验证通过：28/28 绑定、463 题、守卫保留
- 新建 ci/upload.js（miniprogram-ci 2.1.31 已装到隔离 workspace），等用户提供 private.key 即可上传
- 更新过时文案：26章→28章、433题→463题

## v1.0.0 开发版上传成功
- 密钥已配置（ci/private.key，600 权限）；首次被 IP 白名单拦截（-10008，出口 IP 117.103.131.138），用户关闭白名单后上传成功
- 版本 1.0.0 "首次上传：28章可视化教程+463题题库，付费解锁框架（暂全免费）"，robot=1
- 包体积：content-a 507KB / b 748KB / c 691KB / d 480KB / quiz 247KB / 主包 25KB / 总 2.7MB
- 后续迭代流程：改源头 → node tools/build-miniprogram.js → node ci/upload.js <ver> <desc>

## 🔴 修复致命 Bug：页面全空白（v1.0.1）
- 根因：构建脚本生成的代码大量 require('./*.json')，**小程序 require 不支持 json 文件**，页面模块加载即崩 → 全部页面空白（小程序此前从未真正跑通过）
- 报错：`module 'data/chapters.json.js' is not defined`（用 miniprogram-automator 自动化抓到）
- 修复：构建脚本改为输出 .js 数据模块（module.exports = {...}）：chapters.js、quiz-index.js、chXX.js 题库、<slug>.js 章节；index.js 元数据内联
- 同步修改：pages/index|quiz|mine/*.js、tools/mp-templates/reader|runner
- 踩坑：index.js 生成时的 existsSync 过滤器漏改为 .js，导致首次重建后分包索引为空，二次修复
- 自动化验证：首页 28 条/题库 28 套 463 题/阅读页 html 正常渲染，运行时零报错
- v1.0.1 已上传微信后台
- 诊断经验：cli auto 的自动化端口易受 IDE 残留进程干扰，先 pkill -9 -f wechatwebdevtools 再 automator.launch

## v1.1.0 UI 大改版（课程页 + 闯关答题游戏）
- 课程页重做：深色渐变 Hero（统计栏28章/463题/已读+进度条）、继续学习卡、5 阶段分组（入门认知/核心机制/工具与能力/平台实战/工程化进阶，各配主题色渐变数字块）、卡片入场动画
- 题库页 → 闯关地图：紫色 Hero（累计星数/总积分/已通关）、路径式关卡列表（节点+连线，通关✓/解锁紫/锁定🔒）、星级评级（≥90%三星/70%两星/60%一星通关）、锁定关可弹窗"直接挑战"
- runner 全面游戏化：5 生命❤、30 秒倒计时条（<10s 变红闪烁）、连击🔥（≥3 连击飘分）、得分（基础10+速度奖励+连击奖励）、单选点击即判定、判定横幅、解析卡滑入、错题 shake、生命耗尽强制结算
- 结算页：星级逐个弹出动画、得分、破纪录徽章、答题卡网格、错题复盘（面试要点：题目+正确答案+解析）、"下一关"直通按钮
- store.js 扩展：calcStars/isPassed/getGameStats、saveQuizResult 记录 bestScore/stars
- mine 页改为 已读/总积分/星数 三卡
- **踩坑1：WXML 模板不支持方法调用**（answer.indexOf 全部失效→所有选项显示"正确"），已改为 JS 预计算 optionViews 视图模型
- **踩坑2：IDE 不感知外部 build 的文件变更**，改完必须 cli quit 重启 IDE 才会编译新产物；残留进程占 49506 端口会卡死启动（lsof -ti:49506 | xargs kill -9）
- 自动化验证全过：首页 5 阶段 28 卡、地图 28 关、答题页判定/解析/选项状态渲染正确、零运行时报错；v1.1.0 已上传后台

## v1.1.1 章节流程图手机端适配
- 根因：构建脚本 extractJsonConfig 用 JSON.parse 解析 JS 对象字面量（key 无引号）→ 必然失败 → 27 章流程图全部退化为占位文本"小程序端暂省略"
- 修复 1：新写 jsLiteralToJson 逐字符状态机（无引号 key 加引号、单双引号串、注释、尾逗号；踩坑：key 后冒号重复输出、尾逗号后跟行内注释需跳过注释再判断）
- 修复 2：extractJsonConfig 改用正则匹配 `FlowChart.render\s*\(\s*['"]`（原来 indexOf 匹配到 `typeof FlowChart !== 'undefined' && FlowChart.render` 后面的 if 块花括号）
- 修复 3：新增 data-flowchart 内联 JSON 属性支持（部分章节用此方式而非 JS 调用）
- 修复 4：innerHTML 自绘容器兜底（ch10/17/18/19 用 container.innerHTML 模板）→ 提取 h3/strong+p 转信息步骤卡
- convertFlowchart 重写：竖向流程卡（start 蓝/process 白/tool 橙/end 绿/decision 黄 按类型配色）、带标签连接箭头、沿 edges 走主链、旁路节点追加
- reader.wxss 补齐全部 rich-text 内容样式：标题层级/代码块(深色)/引用/表格(可横滑)/信息卡/对比双栏/步骤列表/流程卡
- 最终：24 章 775 个流程节点 + 8 章 27 步骤卡，占位归零；截图验证渲染完美；v1.1.1 已上传

## v1.1.2 正文排版优化
- 根因 1：卡片(div.card)内表格未走顶层 table 分支 → 120/162 个表格无 mp-table class，文字挤成竖条（截图中表格渲染混乱的元凶）
- 修复：构建脚本 card 分支里对 inner.find('table') 也加 mp-table class；162 个表格全覆盖
- 排版调整（reader.wxss）：正文 29→28rpx、行距 1.8→1.75、字间距 0.5rpx；标题层级拉大（h2 33rpx 粗+10rpx 边条 / h3 30rpx 浅边 / h4 29rpx）；卡片内 p/li/table 更紧凑（27rpx/23rpx）；表格 th nowrap+底色、td min-width 120rpx、圆角边框、卡片内表格更窄
- 截图验证：表格横滑+对齐正常，段落密度舒适；v1.1.2 已上传

## v1.1.3 字体优化
- 根因：全项目未声明 font-family（中文走各机型系统默认，安卓易出现宋体/杂乱默认字体）；且 app.wxss 与 reader.wxss 对 .sh/.mp-card 等同名 class 双处定义互相冲突
- 修复：app.wxss 重写为统一系统字体栈（-apple-system/PingFang SC/HarmonyOS Sans SC/MiSans/OPPO Sans...）+ 抗锯齿；删除 app.wxss 中与 reader 冲突的旧内容块样式（确认主包页面无依赖）
- reader/runner/index/quiz/mine 五个页面 wxss 全部加同款字体栈；代码块指定 SF Mono/Menlo 等宽
- 截图验证字体锐利清晰；v1.1.3 已上传

## v1.1.4 品牌图标（14:15）
- 用户提供微信发来的 X Logo（256px .dat 实为 .ico）
- 全套图标生成（/tmp/gen_icons.py，Pillow）：favicon.ico 升级多尺寸 16~256、apple-touch-icon 180、assets/site-logo.png 512
- tabBar 图标 81px×6 张（course/quiz/mine + -active），工具自绘剪影：书本/对勾圆环/人像，灰 #8a94a6 / 蓝 #2563eb
- app.json tabBar 加 iconPath/selectedIconPath；index.wxml Hero 加 hero-logo（白底圆角卡）+ index.wxss 样式
- 构建脚本不清 miniprogram/assets，图标直接放 assets/tab/ 安全
- 上传 v1.1.4 成功（注意 ci/upload.js 必须带 NODE_PATH 指向 workspace node_modules，否则报 miniprogram-ci 缺失）
- 提醒用户：小程序头像需在 mp.weixin.qq.com 后台手动传 144×144

## v1.1.5 阅读排版精修 + 代码块修复（14:45）
- 根因：章节代码块是 `<div class="code-block"><pre>` 包裹（28章268处），构建脚本只认顶层 pre → 代码被拍平成一行段落文本。多语言版为 `.code-lang-pane`（data-label/language）结构
- 修复：build-miniprogram.js 新增 code-block 分支 → 生成 `<div class="mp-codeblock"><view class="mp-code-lang">语言</view><pre class="code">…</pre></div>`；取第一个 pane（多语言只取首个，避免重复）
- 顺带：顶层 p 分支改为保留 el.html()（strong/em/code 行内标记不再丢），产物校验：无未闭合标签、无 style/img/a 残留
- 排版精修 reader.wxss：正文 30→26rpx、标题 38/33→34/30 逐级递减（h3 27、h4 25）、行距 1.8、间距节奏统一；行内 code 琥珀色胶囊底；代码块深色卡 #1c2230 + 语言角标；引用/卡片/表格/步骤全部收一号并去重影
- 验证：自动化截图（reader top/code/code2）正常，303 个 pre 全部生成，全部章节标签闭合校验通过
- 上传 v1.1.5 成功（__FULL__ 2075KB，比上版减 764KB——代码块不再重复拍平文本）

## v1.1.6 代码语法高亮（14:55）
- 需求：代码块各类型文件缺高亮、看不清
- 方案：rich-text 不能跑 JS 高亮库 → 构建期 tokenize（build-miniprogram.js `highlightCode()`）：逐字符状态机，注释(#)/*/、字符串（含三引号/模板串）、数字、关键字（KW_COMMON/JS/SHELL 三集合）、函数名(后跟"(")、类名(大驼峰) → tok-k/s/n/c/f/t 六类 span
- 配色（reader.wxss .tok-*）：关键字紫#c792ea、字符串绿#a5d6a7、数字橙#f78c6c、注释灰斜体#7d8799、函数蓝#82aaff、类型黄#ffcb6b
- **重要发现：rich-text 只解析白名单标签，`<view>` 在 rich-text 内不渲染**（生成 HTML 里 178 个 view 全部换成 div/span；语言角标 view→div 修复）
- code-block 无 lang 提示时默认按 python 高亮（内容以 Python 为主）
- 体积 2075KB→2535KB（span 标记开销），可接受
- 上传 v1.1.6 成功

## v1.2.0 模拟面试改版（答题 → 面试官对话）
- 需求：答题闯关改成模拟面试，聊天对话感
- 面试官池 miniprogram/utils/interview.js：林致远/苏晚/陈默（按场次序号轮换，各配开场白/点评/纠正话术池），评级映射 星级→S/A/B/C（沿用 ≥90/70/60% 线）
- runner 全重写（tools/mp-templates/quiz/）：删生命值/倒计时/连击，改 scroll-view 聊天流——面试官气泡（问题/点评/纠正/标准答案卡/阶段性反馈每5题）+ 打字中三点动效 + 我的回答绿色气泡（微信绿 #95ec69）；报告页=评级大字+面试官总评引号引述+逐题表现格+建议复习点
- 关键实现：_say() 队列逐条延迟弹出（打字→气泡），scroll-into-view 用递增 tick 触发；WXML 不支持方法调用仍需 JS 预计算 optionViews（老坑延续）
- 题库 Tab pages/quiz/ 重写为面试大厅：每章一场面试卡（面试官头像/题量/预计时长/评级徽章 S-A-B-C），保留解锁链与 store 兼容（历史记录无需清）
- 文案：reader 章末 CTA「去闯关→去面试」、index footer
- 新增 tools/generate-interview-preview.js → interview-preview.html：手机框内 1:1 可交互预览（复用 interview.js/store.js 源码 + 真实题库前4章72题，支持语速/一键答完/切场次）
- 验证：Node 环境模拟 wx API 完整跑通 ch00(全对→S)/ch05(混合→C)/ch12(全错→C) 对话流+落库；agent-browser 浏览器实测开场→作答→点评→报告全链路渲染正常
- 待办：微信开发者工具真机验证后 ci/upload.js 上传 v1.2.0

## v1.2.1 转换器重写：容器内表格/代码/问答全部丢失（15:00）
- 用户反馈：章节里表格在小程序没渲染出来
- 根因（比 v1.1.2 更深）：convertChapterHtml 只遍历 body 顶层节点，凡包在容器里的块全被「拍平」——.comparison-table 包的 69 张表、.summary-box/.qa-item 问答、.multi-lang 多语言代码块、.info-box 等，只有 .card 内表格被 v1.1.2 补过
- 修复 1：构建脚本先修 ReferenceError（code-block 分支 first 变量未在外层声明，脚本必崩、分包只剩半截）
- 修复 2：整体重写为递归渲染架构——BLOCK/INLINE_TAGS 分类 + DROP_SELECTOR（script/svg/quiz/lang-tabs 等网页专属节点）+ renderFlow（行内内容聚合为 p、块级递归）+ renderContainer（卡片/代码/QA/装饰盒分类）+ sanitizeFragment（去 style/id/data-*/事件、解包非白名单标签）+ normalizeTables（全局 232 张表统一 mp-table）+ codeBlockHtml（兼容 code-lang-pane 与 lang-pane 两种多语言结构，取 active/首个 pane——多语言只保留一种语言是设计决策，TS/Go/Java 版不进小程序）
- 卡片 accent 色：网页版 border-left: var(--color-xxx) → mp-accent-success/warning/error/info/accent 映射
- reader.wxss 新增：mp-accent-* 左色条、mp-qa-q 问句行、mp-qa 答案底色、mp-table-wrap 滚动外壳、卡片嵌套虚线降级
- 验证：构建通过 28 章 2.57MB；生成物 <table 总数 232/232 全保留（老版仅 28）；分句抽查 RAG/技能/部署/推理/评估等关键对比表内容全部在位
- 注意：并行会话在改此文件（generate-interview-preview.js 同期出现），改构建脚本前先确认最新内容；本次用 python 行区间替换避免冲突

## v1.1.7 阅读页视觉精修（15:20）
- 用户反馈：文字内容渲染不够细腻好看
- 诊断：构建脚本已是递归渲染新架构（renderFlow/renderContainer/renderNode + CARD_RE/CODE_RE 等 regex 分派），但 `banner-title` 不在 CARD_TITLE_SEL → info/tip/warning-banner 内的标题 div 被再次当成 card 嵌套（mp-card 套 mp-card，标题变正文）
- 修复1：CARD_TITLE_SEL 加 `.banner-title`；新增 BANNER_ACCENT 映射（info-banner→info蓝、tip-banner→success绿、warning-banner→warning橙）
- 修复2：reader.wxss 卡片视觉重做——mp-card 带左侧 6rpx 语义色条 + 同系浅底（success 绿底/warning 橙底/info 蓝底），去紫色标题改中性深灰，卡片内 code 胶囊加深
- 截图确认：本章目标（蓝）、最小闭环（绿）、tip 提示（绿）色条+底色语义正确，标题层级清爽
- 上传 v1.1.7 成功
2026-09-21 阅读页书籍级排版改版：reader.wxss 正文 26→28rpx、行距 1.8→2.0、段距/留白全面加大、两端对齐；app.wxss 全局行距 1.8；已重建并验证各分包同步
2026-09-21 阅读页新增：夜间模式（CSS 变量整页覆盖 + 导航栏联动）+ Aa 设置面板（悬浮按钮/三档字号/开关，偏好存 dsh_reader_pref）+ 段落间距进一步拉开（行距2.05、段距26rpx、标题前距68-72rpx）；模板 reader.{wxml,js,wxss} 全部更新并重建验证

## 模拟面试升级：面试官风格分类 + 大厂真题场
- utils/interview.js：面试官池扩到 7 人，四种风格（sharp犀利/gentle温柔/boss大厂实战/steady稳重），新增 pickByStyle(styleId, seed)；pick(seed) 保留兼容
- 题库新增三个独立大厂真题场（data/quiz-bank.json：bytedance/meituan/jd 各 10 题，源自检索的真实面经：字节 Decoder-only/RLHF/Agent 记忆，美团 RAG 排查/工具容错，京东 MHA-MQA-GQA/电商场景/资源调度）
- 构建脚本 BOSS_QUIZZES 常量：注册为大厂场次（key=boss_*，isBoss=true，常驻解锁，不参与解锁链）
- 题库页（pages/quiz）新增风格选择卡（可点选/取消），场次卡显示面试官+风格徽章；runner 支持 ?style= 参数（模板 tools/mp-templates/quiz/runner.js 已同步）

## 小程序文字排版细化（第二轮）
- 构建脚本 highlightCode 增强：Python 三引号字符串整体着色（不再切成三段碎片）、@decorator、$VAR、JSON key、Go/Java 关键字集；URL 协议头 http:// 不再误判为注释；Shell 单引号包裹 JSON（-d '{...}'）跨行吃引号体；无 data-lang 的 code-block 从 code-label 文本推断语言（"Shell — xxx" → shell）
- reader.wxss：行内 code 从橙色改为蓝紫主题胶囊（#4353c9 on #eef1fb）；代码块 pre 改为 white-space:pre + overflow-x:auto（不再 break-all 硬切英文）；字号三档作用域扩到列表/引用/问答/卡片/对比卡/步骤描述（用 .fs-* 前缀统一缩放）；夜间 h3/h4 标题色提亮
- 全量构建验证：299 个代码块 0 高亮碎片，566 个行内 code 保留完整

## 提交
- commit 39a39ab：代码高亮精细化 + 排版主题统一 + 大厂真题场，63 文件（构建产物 + 模板 + quiz 数据）
