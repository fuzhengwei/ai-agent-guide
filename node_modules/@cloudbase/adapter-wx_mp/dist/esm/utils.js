var MINI_PROGRAM_EMPTY_CONTENT_TYPE = " ";
export function resolvePutHeaders(headers) {
    if (headers === void 0) { headers = {}; }
    var result = {};
    var contentType;
    Object.keys(headers).forEach(function (key) {
        if (key.toLowerCase() === "content-type") {
            contentType = headers[key];
            return;
        }
        result[key] = headers[key];
    });
    var trimmed = typeof contentType === "string" ? contentType.trim() : contentType;
    result["content-type"] = trimmed || MINI_PROGRAM_EMPTY_CONTENT_TYPE;
    return result;
}
function isBlobLike(value) {
    return typeof Blob !== "undefined" && value instanceof Blob;
}
function toUploadFile(value) {
    if (typeof File !== "undefined" && value instanceof File) {
        return value;
    }
    if (typeof File !== "undefined") {
        return new File([value], "file", { type: value.type || "" });
    }
    return value;
}
export function resolveUploadFileSource(file) {
    if (typeof file === "string" && file) {
        return { filePath: file };
    }
    if (isBlobLike(file)) {
        var uploadFile = toUploadFile(file);
        return {
            file: uploadFile,
            files: [{ name: "file", file: uploadFile }],
        };
    }
    throw new Error("upload file path or File is required");
}
function isBinaryLike(value) {
    if (value == null) {
        return false;
    }
    if (isBlobLike(value)) {
        return true;
    }
    if (typeof ArrayBuffer !== "undefined") {
        if (value instanceof ArrayBuffer) {
            return true;
        }
        if (typeof ArrayBuffer.isView === "function" && ArrayBuffer.isView(value)) {
            return true;
        }
    }
    return false;
}
function toRequestBody(value) {
    if (!isBlobLike(value)) {
        return Promise.resolve(value);
    }
    if (typeof value.arrayBuffer === "function") {
        return value.arrayBuffer();
    }
    return new Promise(function (resolve, reject) {
        var reader = new FileReader();
        reader.onload = function () { return resolve(reader.result); };
        reader.onerror = function () { return reject(reader.error || new Error("read blob failed")); };
        reader.readAsArrayBuffer(value);
    });
}
export function readUploadBody(file) {
    if (isBinaryLike(file)) {
        return toRequestBody(file);
    }
    if (typeof file !== "string" || !file) {
        return Promise.reject(new Error("upload file path or file content is required"));
    }
    if (typeof wx.getFileSystemManager !== "function") {
        return Promise.reject(new Error("getFileSystemManager is not supported; cannot PUT a local file path"));
    }
    var fs = wx.getFileSystemManager();
    if (typeof fs.readFile === "function") {
        return new Promise(function (resolve, reject) {
            fs.readFile({
                filePath: file,
                success: function (res) { return resolve(res.data); },
                fail: reject,
            });
        });
    }
    if (typeof fs.readFileSync === "function") {
        return Promise.resolve(fs.readFileSync(file));
    }
    return Promise.reject(new Error("readFile is not supported in this environment"));
}
